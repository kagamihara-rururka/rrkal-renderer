[project]
name = "rrkal-renderer"
version = "0.2.0"
description = "Standalone renderer for RRKAL artifacts"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
rrkal-renderer = "rrkal_renderer.cli:main"
